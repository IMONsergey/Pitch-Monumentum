name: Desktop Full macOS x64

on:
  workflow_dispatch:

concurrency:
  group: pitch-desktop-full-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-x64-dmg:
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: node scripts/package-desktop-full.mjs
      - name: Verify Intel executable
        shell: bash
        run: |
          set -euo pipefail
          APP_BIN="$(find dist-desktop-full -type f -path '*.app/Contents/MacOS/Pitch Monumentum' -print -quit)"
          test -n "$APP_BIN"
          file "$APP_BIN" | tee desktop-full-architecture.txt
          grep -q 'x86_64' desktop-full-architecture.txt
      - name: Hash DMG
        shell: bash
        run: |
          set -euo pipefail
          DMG="$(find dist-desktop-full -maxdepth 2 -type f -name '*.dmg' -print -quit)"
          test -n "$DMG"
          shasum -a 256 "$DMG" | tee desktop-full-sha256.txt
          echo "DMG_PATH=$DMG" >> "$GITHUB_ENV"
      - uses: actions/upload-artifact@v4
        with:
          name: Pitch-Monumentum-Full-mac-x64
          path: |
            ${{ env.DMG_PATH }}
            desktop-full-architecture.txt
            desktop-full-sha256.txt
          if-no-files-found: error
