import type { DeckDocument, SceneElement, TextRun } from "../../deck-model/src/index.js";
import type { MotionDocument, SlideMotion } from "../../motion-engine/src/index.js";

export interface WebExportAsset {
  assetId: string;
  mimeType: string;
  base64: string;
}
export interface WebExportResult { html: string; warnings: string[]; }

function esc(value: unknown): string { return String(value ?? "").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"); }
function cssColor(value: string | undefined, fallback = "transparent"): string { return value && /^#[0-9a-f]{6}$/i.test(value) ? value : fallback; }
function style(parts: Array<string | undefined | false>): string { return parts.filter(Boolean).join(";"); }
function base(element: SceneElement): string {
  const g=element.geometry; return style([`position:absolute`,`left:${g.x}px`,`top:${g.y}px`,`width:${g.width}px`,`height:${g.height}px`,`z-index:${element.zIndex}`,`opacity:${element.opacity??1}`,`transform:rotate(${g.rotation??0}deg)`,`transform-origin:center center`,element.locked?"pointer-events:none":undefined]);
}
function run(run: TextRun): string {
  return `<span style="${style([run.bold?"font-weight:700":undefined,run.italic?"font-style:italic":undefined,run.underline?"text-decoration:underline":undefined,run.color?`color:${cssColor(run.color,"#111111")}`:undefined,run.fontFamily?`font-family:${esc(run.fontFamily)},sans-serif`:undefined,run.fontSizePt?`font-size:${run.fontSizePt*160/72}px`:undefined,run.letterSpacingPt!==undefined?`letter-spacing:${run.letterSpacingPt*160/72}px`:undefined])}">${esc(run.text)}</span>`;
}
function textElement(element: Extract<SceneElement,{type:"text"}>): string {
  const paragraphs=element.paragraphs.map(p=>`<div style="${style([`text-align:${p.align??"left"}`,p.lineSpacing?`line-height:${p.lineSpacing}`:undefined,p.spaceBeforePt?`margin-top:${p.spaceBeforePt*160/72}px`:undefined,p.spaceAfterPt?`margin-bottom:${p.spaceAfterPt*160/72}px`:undefined])}">${p.runs.map(run).join("")}</div>`).join("");
  const justify=element.verticalAlign==="middle"?"center":element.verticalAlign==="bottom"?"flex-end":"flex-start";
  return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};display:flex;flex-direction:column;justify-content:${justify};overflow:hidden;white-space:pre-wrap">${paragraphs}</div>`;
}
function shapeElement(element: Extract<SceneElement,{type:"shape"}>): string {
  const fill=(element.fillPaint as any)?.kind==="solid"?(element.fillPaint as any).color:element.fill;
  const stroke=element.stroke?`border:${element.stroke.widthDU}px ${element.stroke.dash==="dash"?"dashed":element.stroke.dash==="dot"?"dotted":"solid"} ${cssColor(element.stroke.color)}`:undefined;
  if(element.shape==="triangle") return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)}"><svg width="100%" height="100%" viewBox="0 0 ${element.geometry.width} ${element.geometry.height}"><polygon points="${element.geometry.width/2},0 ${element.geometry.width},${element.geometry.height} 0,${element.geometry.height}" fill="${cssColor(fill)}" ${element.stroke?`stroke="${cssColor(element.stroke.color)}" stroke-width="${element.stroke.widthDU}"`:""}/></svg></div>`;
  if(element.shape==="custom"&&(element.svgPath||element.pathData)) { const path=element.svgPath||""; return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)}"><svg width="100%" height="100%" viewBox="0 0 ${element.geometry.width} ${element.geometry.height}"><path d="${esc(path)}" fill="${cssColor(fill)}" ${element.stroke?`stroke="${cssColor(element.stroke.color)}" stroke-width="${element.stroke.widthDU}"`:""}/></svg></div>`; }
  return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};background:${cssColor(fill)};${stroke??""};border-radius:${element.shape==="ellipse"?"50%":element.shape==="roundRect"?`${element.radiusDU??18}px`:`${element.radiusDU??0}px`}"></div>`;
}
function frameElement(element: Extract<SceneElement,{type:"frame"}>): string { const fill=(element.fillPaint as any)?.kind==="solid"?(element.fillPaint as any).color:element.fill;return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};background:${cssColor(fill)};border-radius:${element.radiusDU??0}px;${element.stroke?`border:${element.stroke.widthDU}px solid ${cssColor(element.stroke.color)}`:""};${element.clipContent?"overflow:hidden":"pointer-events:none"}"></div>`; }
function lineElement(element: Extract<SceneElement,{type:"line"}>): string { const s=element.start,e=element.end;return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};overflow:visible"><svg width="100%" height="100%" viewBox="0 0 ${Math.max(1,element.geometry.width)} ${Math.max(1,element.geometry.height)}" overflow="visible"><line x1="${s[0]}" y1="${s[1]}" x2="${e[0]}" y2="${e[1]}" stroke="${cssColor(element.stroke.color,"#111111")}" stroke-width="${element.stroke.widthDU}" ${element.stroke.dash==="dash"?'stroke-dasharray="8 6"':element.stroke.dash==="dot"?'stroke-dasharray="2 5"':""}/></svg></div>`; }
function imageStyle(element: Extract<SceneElement,{type:"image"}>) { const crop=element.crop??{left:0,top:0,right:0,bottom:0};const vw=Math.max(.001,1-crop.left-crop.right),vh=Math.max(.001,1-crop.top-crop.bottom);const focal=(element as any).focalPoint??{x:.5,y:.5};const px=Math.max(0,Math.min(1,(focal.x-crop.left)/vw))*100,py=Math.max(0,Math.min(1,(focal.y-crop.top)/vh))*100;return `position:absolute;left:${-(crop.left/vw)*100}%;top:${-(crop.top/vh)*100}%;width:${100/vw}%;height:${100/vh}%;object-fit:${element.fit==="stretch"?"fill":element.fit};object-position:${px}% ${py}%`;
}
function mediaElement(element: Extract<SceneElement,{type:"image"|"icon"}>,assets:Record<string,WebExportAsset>): string { const asset=assets[element.assetId];const uri=asset?`data:${asset.mimeType};base64,${asset.base64}`:"";if(element.type==="icon")return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};overflow:hidden"><img src="${uri}" alt="" style="width:100%;height:100%;object-fit:contain"/></div>`;const clip=(element as any).clipShape;const clipPath=clip==="ellipse"?"clip-path:ellipse(50% 50% at 50% 50%)":undefined;return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};overflow:hidden;border-radius:${element.cornerRadiusDU??0}px;${clipPath??""}"><img src="${uri}" alt="${esc(element.alt??"")}" style="${imageStyle(element)}"/></div>`; }
function tableElement(element: Extract<SceneElement,{type:"table"}>): string { return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};overflow:hidden"><table style="width:100%;height:100%;border-collapse:collapse;font:18px/1.2 Inter,sans-serif">${element.rows.map(row=>`<tr>${row.map(cell=>`<td colspan="${cell.colspan??1}" rowspan="${cell.rowspan??1}" style="border:1px solid #D8DDE3;padding:8px">${esc(cell.text)}</td>`).join("")}</tr>`).join("")}</table></div>`; }
function chartElement(element: Extract<SceneElement,{type:"chart"}>): string { const chart=element.chart;const values=chart.series.flatMap(s=>s.values);const max=Math.max(1,...values.map(v=>Math.abs(v)));if(["column","bar"].includes(chart.chartType)){const series=chart.series[0]?.values??[];return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};display:flex;align-items:flex-end;gap:8px;padding:12px;border:1px solid #E0E4E8">${series.map(v=>`<div style="flex:1;height:${Math.abs(v)/max*90}%;background:#4B75FF;min-width:3px"></div>`).join("")}</div>`;}return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};display:grid;place-items:center;border:1px solid #D8DDE3;color:#6B7280;font:18px Inter,sans-serif">${esc(chart.chartType.toUpperCase())} · ${esc(chart.insightStatement)}</div>`; }
function renderElement(element:SceneElement,assets:Record<string,WebExportAsset>,warnings:string[],slideId:string):string { if(element.type==="text")return textElement(element);if(element.type==="shape")return shapeElement(element);if(element.type==="frame")return frameElement(element);if(element.type==="line")return lineElement(element);if(element.type==="image"||element.type==="icon")return mediaElement(element,assets);if(element.type==="table")return tableElement(element);if(element.type==="chart")return chartElement(element);if(element.type==="group")return "";warnings.push(`${slideId}:${element.id} ${element.type} uses web fallback metadata and is not fully rendered`);return `<div class="pitch-el" data-pitch-id="${esc(element.id)}" style="${base(element)};display:grid;place-items:center;border:1px dashed #AEB6C0;color:#7A8490">${esc(element.type)}</div>`; }
function motionFor(motion:MotionDocument|undefined,slideId:string):SlideMotion|undefined { return motion?.slides.find(slide=>slide.slideId===slideId); }
function motionJson(motion:MotionDocument|undefined) { return JSON.stringify(motion??{schemaVersion:"0.1",deckId:"",slides:[]}).replace(/</g,"\\u003c"); }
export function exportStandaloneWeb(deck:DeckDocument,assets:Record<string,WebExportAsset>,motion?:MotionDocument):WebExportResult { const warnings:string[]=[];for(const slide of deck.slides)for(const element of slide.scene)if((element.type==="image"||element.type==="icon")&&!assets[element.assetId])warnings.push(`${slide.id}:${element.id} missing embedded asset ${element.assetId}`);const slides=[...deck.slides].sort((a,b)=>a.order-b.order).map((slide,index)=>`<section class="pitch-slide ${index===0?"active":""}" data-slide-id="${esc(slide.id)}" data-index="${index}" style="width:${deck.canvas.widthDU}px;height:${deck.canvas.heightDU}px">${[...slide.scene].sort((a,b)=>a.zIndex-b.zIndex).map(el=>renderElement(el,assets,warnings,slide.id)).join("")}<aside class="pitch-notes">${esc(slide.speakerNotes??"")}</aside></section>`).join("");const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(deck.title)}</title><style>*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;background:#090b0e;overflow:hidden;font-family:Inter,system-ui,sans-serif}.pitch-viewport{position:absolute;inset:0;display:grid;place-items:center}.pitch-stage{width:${deck.canvas.widthDU}px;height:${deck.canvas.heightDU}px;position:relative;transform-origin:center}.pitch-slide{position:absolute;inset:0;display:none;background:white;overflow:hidden;box-shadow:0 25px 90px #0008}.pitch-slide.active{display:block}.pitch-el{box-sizing:border-box}.pitch-ui{position:fixed;left:14px;right:14px;bottom:12px;z-index:9999;display:flex;align-items:center;gap:8px;color:#c8cdd4;font:11px system-ui;pointer-events:none}.pitch-ui .bar{height:2px;background:#ffffff25;flex:1}.pitch-ui .bar i{display:block;height:100%;background:#fff;width:0}.pitch-notes{display:none}.pitch-build-hidden{opacity:0!important}.pitch-pulse{animation:pitchPulse .45s ease}@keyframes pitchPulse{50%{transform:scale(1.04)}}@media print{html,body{overflow:visible;background:white}.pitch-viewport{position:static;display:block}.pitch-stage{transform:none!important;width:auto;height:auto}.pitch-slide{position:relative;display:block!important;page-break-after:always;box-shadow:none}}</style></head><body><div class="pitch-viewport"><main class="pitch-stage">${slides}</main></div><div class="pitch-ui"><span id="pitchCounter">1 / ${deck.slides.length}</span><div class="bar"><i id="pitchProgress"></i></div></div><script id="pitchMotion" type="application/json">${motionJson(motion)}</script><script>(()=>{const slides=[...document.querySelectorAll('.pitch-slide')],motion=JSON.parse(document.getElementById('pitchMotion').textContent||'{}');let index=0,buildIndex=0,phases=[];const stage=document.querySelector('.pitch-stage'),counter=document.getElementById('pitchCounter'),progress=document.getElementById('pitchProgress');function fit(){const s=Math.min(innerWidth/${deck.canvas.widthDU},innerHeight/${deck.canvas.heightDU});stage.style.transform='scale('+s+')'}function slideMotion(id){return(motion.slides||[]).find(s=>s.slideId===id)}function compile(builds){const result=[];for(const b of builds||[]){if(!result.length||b.trigger==='onClick')result.push([b]);else result[result.length-1].push(b)}return result}function reset(){const s=slides[index],m=slideMotion(s.dataset.slideId);phases=compile(m?.builds);buildIndex=0;s.querySelectorAll('.pitch-el').forEach(n=>{n.classList.remove('pitch-build-hidden','pitch-pulse');n.style.opacity=''});for(const b of m?.builds||[])if(b.kind==='entrance')for(const id of b.elementIds||[])s.querySelector('[data-pitch-id="'+CSS.escape(id)+'"]')?.classList.add('pitch-build-hidden')}function show(next){if(next<0||next>=slides.length)return;slides[index]?.classList.remove('active');index=next;slides[index].classList.add('active');reset();counter.textContent=(index+1)+' / '+slides.length;progress.style.width=((index+1)/slides.length*100)+'%'}function applyPhase(){if(buildIndex>=phases.length)return false;for(const b of phases[buildIndex++])for(const id of b.elementIds||[]){const n=slides[index].querySelector('[data-pitch-id="'+CSS.escape(id)+'"]');if(!n)continue;if(b.kind==='entrance')n.classList.remove('pitch-build-hidden');else if(b.kind==='exit')n.classList.add('pitch-build-hidden');else{n.classList.remove('pitch-pulse');void n.offsetWidth;n.classList.add('pitch-pulse')}}return true}function next(){if(!applyPhase())show(index+1)}function prev(){show(index-1)}addEventListener('resize',fit);addEventListener('keydown',e=>{if(['ArrowRight',' ','PageDown','Enter'].includes(e.key)){e.preventDefault();next()}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();prev()}if(e.key==='Home')show(0);if(e.key==='End')show(slides.length-1)});addEventListener('click',e=>{if(e.clientX<innerWidth*.25)prev();else next()});fit();show(0)})();</script></body></html>`;if(motion?.slides.some(s=>s.tracks.length))warnings.push("Standalone Web export currently preserves builds but does not play exact keyframe tracks; use Pitch Presenter for full keyframe playback.");return{html,warnings:[...new Set(warnings)]};}
