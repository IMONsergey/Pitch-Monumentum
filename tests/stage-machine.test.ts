import test from "node:test";import assert from "node:assert/strict";import { createPipelineState, markDownstreamStale, canRun } from "../packages/agent-runtime/src/stage-machine.js";
test("stage dependencies and invalidation",()=>{const s=createPipelineState("p","b");assert.equal(canRun(s,"INTAKE"),true);assert.equal(canRun(s,"EVIDENCE"),false);s.stages.INTAKE.state="ready";assert.equal(canRun(s,"EVIDENCE"),true);for(const v of Object.values(s.stages))v.state="ready";const n=markDownstreamStale(s,"EVIDENCE");assert.equal(n.stages.NARRATIVE.state,"stale");assert.equal(n.stages.DESIGN.state,"stale");assert.equal(n.stages.INTAKE.state,"ready");});
