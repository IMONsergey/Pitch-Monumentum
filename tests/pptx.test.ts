import test from "node:test";import assert from "node:assert/strict";import { rm, stat } from "node:fs/promises";import { compileDeckToPptx, duToEmu } from "../packages/pptx/src/index.js";import type { DeckDocument } from "../packages/deck-model/src/index.js";
test("DU maps exactly to standard widescreen PowerPoint units",()=>{assert.equal(duToEmu(1920),12192000);assert.equal(duToEmu(1080),6858000);});
test("minimal compiler creates a non-empty PPTX zip with native objects",async()=>{const out="/tmp/pitchos-minimal.pptx";await rm(out,{force:true});const now=new Date().toISOString();const d:DeckDocument={schemaVersion:"0.1",id:"d",title:"Native test",canvas:{widthDU:1920,heightDU:1080,duPerInch:144,aspectRatio:"16:9"},briefId:"b",narrativeId:"n",designSystemId:"ds",sourceIds:[],claimIds:[],activeBranchId:"main",createdAt:now,updatedAt:now,slides:[{id:"s1",order:0,title:"Slide",archetype:"thesis",semantic:{purpose:"p",takeaway:"Editable native text",questionAnswered:"q",narrativeRole:"r",claimIds:[],evidenceRefs:[],audienceRelevance:"a",density:"sparse"},scene:[{id:"bg",type:"shape",semanticRole:"decoration",geometry:{x:0,y:0,width:1920,height:1080},zIndex:0,origin:"deterministic",exportStrategy:"native",dependencies:[],shape:"rect",fill:"#0D0E11"},{id:"t",type:"text",semanticRole:"title",geometry:{x:150,y:200,width:1400,height:300},zIndex:1,origin:"agent",exportStrategy:"native",dependencies:[],paragraphs:[{runs:[{text:"Editable PitchOS text",fontSizePt:54,bold:true,color:"#FFFFFF"}]}]}],status:"draft",qaIssueIds:[],dependencyIds:[]} ]};const r=await compileDeckToPptx(d,out);assert.equal(r.slideCount,1);assert.equal(r.elementResults.filter(x=>x.strategy==="native").length,2);assert.ok((await stat(out)).size>1000);});
